---
marp: true
theme: default
paginate: true
header: 'Smart Contracts — Clase 2'
footer: 'Intro a Solidity'
style: |
  section { font-size: 24px; }
  section.lead { justify-content: center; text-align: center; }
  section.lead h1 { font-size: 50px; }
  section.lead h2 { font-size: 32px; color: #475569; }
  table { font-size: 19px; }
  th { background: #f1f5f9; }
  svg { display: block; margin: 0.3em auto; }
  pre { font-size: 16px; }
  .key { background: #fef9c3; border-left: 6px solid #ca8a04; padding: 0.3em 0.7em; }
  .seg { color: #64748b; font-size: 20px; letter-spacing: 2px; }
  .nota { color: #b45309; font-weight: 700; }

---

<!-- _class: lead -->
<!-- _paginate: false -->

# Clase 2
## Intro a Solidity

---

## Todavía no publicada

Estas slides se publican **antes de cada clase**. Este archivo es un placeholder:
mantiene el deck listo para `marp` y reserva el lugar en el repo.

Mientras tanto, lo que sí podés hacer:

- **Levantar el entorno** — ver el `README.md` de la raíz
- **Mirar el código** — `ethereum/` y `cardano/` ya están completos para las
  primeras clases

<div class="key">

Para actualizar: reemplazá el contenido de este archivo y hacé `git pull` desde
tu clon. El *front-matter* de arriba ya trae el tema del curso, no hace falta
tocarlo.

</div>
